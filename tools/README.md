# Tools e Script

- `backend/` – Utility Python che interagiscono con il database o i servizi del backend.
- `infra/` – Script shell per avviare/fermare servizi di sviluppo e auto-versioning.

Tutti gli script presenti in origine richiamano automaticamente la nuova posizione, quindi i vecchi comandi continuano a funzionare.

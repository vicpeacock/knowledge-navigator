# Others

- `logs/` – Output generato dallo sviluppo (log backend, log MCP, ecc.).
- `uploads/` – File caricati localmente per test.
- Altri file temporanei (es. PID/Log di MCP Gateway).

Questa directory raccoglie artefatti non di codice che possono essere rigenerati.

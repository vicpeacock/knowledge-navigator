# Dockerfile per ChromaDB su Cloud Run
FROM chromadb/chroma:0.4.18

# ChromaDB usa la porta 8000 di default
EXPOSE 8000

# ChromaDB non richiede configurazione aggiuntiva per Cloud Run
# I dati possono essere persistenti usando Cloud Storage (opzionale)

